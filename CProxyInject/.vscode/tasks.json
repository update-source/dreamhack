{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "docker-compose: debug up",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml up --build --wait -d",
      "problemMatcher": []
    },
    {
      "label": "docker-compose: debug down",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml down",
      "problemMatcher": []
    }
  ]
}
